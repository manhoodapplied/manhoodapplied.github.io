---
interface Props { as?: 'h1' | 'h2' | 'h3'; class?: string; id?: string; }
const { as: Tag = 'h2', class: className = '', id } = Astro.props;
---
<Tag id={id} class:list={['terminal-title', className]}><slot /><span aria-hidden="true">.</span></Tag>
